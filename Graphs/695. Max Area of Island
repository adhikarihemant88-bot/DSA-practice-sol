class Solution {
public:
    int dfs(vector<vector<int>>& grid, int i, int j){
        int n = grid.size();
        int m = grid[0].size();

        if(i < 0 || i >= n || j < 0 || j >= m || grid[i][j] == 0) return 0;

        grid[i][j] = 0;
        return 1+ dfs(grid,i-1,j)+dfs(grid,i,j+1)+dfs(grid,i+1,j)+dfs(grid,i,j-1);
    }

    int maxAreaOfIsland(vector<vector<int>>& grid) {
        int n = grid.size();
        int m = grid[0].size();

        int maxA = 0;
        for(int i = 0; i < n ;i++){
            for(int j = 0; j< m ; j++){
                if(grid[i][j] == 1){
                    maxA = max(maxA,dfs(grid,i,j));
                }
            }
        }
        return maxA;
    }
};
//similarity with no. of islands
class Solution {
public:
    int ans =0;
    void dfs(vector<vector<int>>& grid,vector<vector<int>>& v,int x,int y,int& l) {
        vector<int> nx = {0,0,-1,1};
        vector<int> ny = {1,-1,0,0};
        int m=grid.size();
        int n =grid[0].size();
        v[x][y] = 1;
        //cout<<"for "<<x<<" "<<y<<" l is "<<l <<endl;
        ans = max(ans,l);
        for(int i=0;i<4;i++){
            int x1 = x+nx[i];
            int y1 = y +ny[i];
            if(x1>=0 && y1>=0 && x1<m && y1<n && grid[x1][y1] == 1 && v[x1][y1] ==0) {
                l++;
                dfs(grid,v,x1,y1,l);
            }
        }
    }
    int maxAreaOfIsland(vector<vector<int>>& grid) {
        int m=grid.size();
        int n =grid[0].size();
        vector<vector<int>> v(m,vector<int>(n,0));
       
        for(int i =0;i<m;i++) {
            for (int j=0;j<n;j++) {
                if(v[i][j] == 0 && grid[i][j] == 1){
                    cout<<"new "<<endl;
                    int l =1;
                    dfs(grid,v,i,j,l);
                }
            }
        }
        return ans;
    }
};
